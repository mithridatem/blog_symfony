{% extends 'base.html.twig' %}

{% block title %}Liste Categories{% endblock %}

{% block body %}
    <h4 style="margin-left:10px">Liste des catégories :</h4>
    {% for tab in category %}
        <div class="alert alert-dismissible alert-secondary">
            <a href="/showcategory/{{tab.id}}">{{ tab.title }}</a>
        </div>
    {% endfor %}
{% endblock %}
